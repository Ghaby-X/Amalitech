apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    uid: prometheus_ds
    isDefault: true
    editable: false
    jsonData:
      manageAlerts: true
      timeInterval: 15s
