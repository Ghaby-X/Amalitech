# TaskFlow project files (fixture, not a deliverable)

TaskFlow is fictional. These two files stand in for the "provided sample
project files and specs" the lab scenario describes, and are what the
generated documentation is fact-checked against:

- [`specs/api-contract.md`](specs/api-contract.md): the authoritative
  API contract. Source of truth when it conflicts with anything below.
- [`specs/feature-notes.md`](specs/feature-notes.md): messy, scattered
  developer notes with a few deliberate inconsistencies (stale
  terminology, an unshipped field, a QA misunderstanding), the same kind
  of noise described in the scenario.

Neither file is part of the submitted documentation. They exist so the
prompt history in [`../prompt-history.md`](../prompt-history.md) has
real facts to check the AI's output against.
